# Optional: input CSV path (default: ./data/ig_handles.csv). Use name column for DuckDuckGo lookup.
# INPUT_CSV=relative/or/absolute/path.csv
# Requests per second (inverse of delay in seconds). 0.2 = 1 request per 5 seconds.
IG_RPS=0.2
# Fetch timeout in ms (AbortController).
IG_FETCH_TIMEOUT_MS=15000
# Number of retries with exponential backoff.
IG_RETRIES=3
# Optional: custom User-Agent for Instagram requests (leave empty for default).
IG_USER_AGENT=
